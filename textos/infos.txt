cd ListaCompras
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
venv\Scripts\Activate.ps1

ou

venv\Scripts\activate


gera SECRET_KEY: from secrets import token_hex
print(token_hex(32))
com o comando python, no terminal

pra add o .env no app: pip install python-dotenv

no terminal pra atualizar o bd e recriar -->

from app.py import app, db
with app.app_context():
    db.create_all()


git config --global user.name "nome"
git config --global user.email "@gmail.com"


rodar o fastapi servidor ASGI (como uvicorn)
python -m uvicorn appfastapi:app --reload --port 8000


config.py
DATABASE_URL = os.getenv('DATABASE_URL', 'postgresql://postgres:123@localhost:5432/shopping_cart_db')

rodar o flask (servidor web tradicional)
python app.py 

cd backend
uvicorn main:app --reload --port 8000

uvicorn backend.main:app --reload --port 8000

cd frontend
python app.py --port 5000
venv\Scripts\python.exe -m frontend.app

Gerar secret key 
import secrets
print(secrets.token_urlsafe(32))