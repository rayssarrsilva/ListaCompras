senha: Rays64p50
rds listacomoras-db: NyS3HSkklRtIkuod
nome: postgres

-----------------
🔹 ETAPA 1: Criar o PostgreSQL no RDS (Free Tier) CHECK (ec2 regra de negocio tcp port ip4 configurada TBM)
🔹 ETAPA 2: Criar uma instância EC2 (Free Tier) para rodar backend + frontend FEITO
🔹 ETAPA 3: Conectar na EC2 e instalar tudo
🔹 ETAPA 4: Ajustar seu código para usar o RDS (não Supabase)
🔹 ETAPA 5: Iniciar os apps e testar
------------------

https://github.com/rayssarrsilva/ListaCompras.git
# Atualizar sistema
sudo apt update -y && sudo apt upgrade -y

# Instalar Python, pip, git
sudo apt install python3 python3-pip git -y

# Clonar seu projeto
git clone https://github.com/rayssarrsilva/ListaCompras.git
cd ListaCompras

# Instalar backend
cd backend
pip3 install -r requirements.txt

# Instalar frontend
cd ../frontend
pip3 install -r requirements.txt
----------------
✅ Depois disso, sempre que for rodar o app, ative o ambiente virtual primeiro com:
: source venv/bin/activate
---------------------
Definir as variaveis de ambiente

export DATABASE_URL="postgresql://postgres:NyS3HSkklRtIkuod@listacompras-db.c32gauo20gy1.us-east-2.rds.amazonaws.com:5432/postgres"
export SECRET_KEY="listacompras"

git clone https://github.com/rayssarrsilva/ListaCompras.git 
cd ListaCompras
cd ~/ListaCompras

✅ Use a mesma senha do Postgres que está no seu docker-compose.yml.

SECRET_KEY=v3CRWdfVwd8GTGDcF6H4hmzCLlSDpsIcV4JuEkyBmQk
DATABASE_URL=postgresql://postgres:123@postgres:5432/listacompras

---------------------
cd ~/ListaCompras
git pull
docker-compose down
docker-compose up -d --build